# My attempy (try) of OBI training

Sometimes it works. Sometimes it becomes a learning experience.

Run `make list` to see the exercises, or execute one with:

```sh
make run FILE=cpp/staircase_dp.cpp
```
